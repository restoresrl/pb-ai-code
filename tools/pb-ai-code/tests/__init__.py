"""Unit tests for pb-ai-code.

The merge, the plan builder, the marker renderer, path validation and the
dialect emitter live here; the cross-cutting end-to-end tests stay in the
repository's root ``tests/``, where they already have an identity worth
keeping.
"""
